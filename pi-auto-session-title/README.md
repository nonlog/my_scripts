# Pi Auto Session Title — moved

The maintained Pi extension lives at:

https://github.com/nonlog/pi-auto-session-title

Install with:

```powershell
pi install git:https://github.com/nonlog/pi-auto-session-title
```

This path is retained only as a compatibility pointer.
