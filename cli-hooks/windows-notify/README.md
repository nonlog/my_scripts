# Windows CLI Notify — moved

The shared Codex CLI / Claude Code / Pi notification integration now lives at:

https://github.com/nonlog/windows-cli-notify

Run `pwsh -NoProfile -File .\install.ps1` from the new repository. Pi is now installed as the Git package `git:https://github.com/nonlog/windows-cli-notify` instead of a persistent local extension copy.
