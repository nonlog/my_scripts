# Claude Code Auto Session Title — retired

Claude Code now provides built-in AI conversation titles, so the custom hook is retired.

The migration note has moved to [`docs/retired/claude-auto-session-title.md`](../docs/retired/claude-auto-session-title.md). This path is retained only for compatibility.
