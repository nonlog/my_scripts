# Codex Auto Thread Title — moved

This maintained hook now lives in its own repository:

https://github.com/nonlog/codex-auto-thread-title

Install from the new repository with `python .\install.py`. This path is retained only so old links do not break.
